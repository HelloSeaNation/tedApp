import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
    font: {
        heading: "Coiny",
        body: "Marmelad",
    },
})

export default theme;   // export theme object for use in other components